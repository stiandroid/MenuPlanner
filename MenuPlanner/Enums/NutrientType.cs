namespace MenuPlanner.Enums
{
    public enum NutrientType
    {
        Fats,                       // Fett totalt (metter + enumettet + flerumettet)
        SaturatedFats,              // Stearinsyre, palmitinsyre, laurinsyre ...
        MonoUnsaturatedFats,        // Oljesyre, ...
        PolyUnsaturatedFats,        // Omega-3, Omega-6, ...
        TransFat,                   // Transfett
        Carbohydrates,              // Karbohydrater totalt (sukkerarter + polyoler + stivelse)
        Sugars,                     // Sukkerarter
        Polyols,                    // Polyoler
        Starch,                     // Stivelse
        CarbohydrateDietaryFiber,   // Kostfiber
        Protein,                    // Aminosyrer, ensymer, ...
        Salt,                       // Salt
        Vitamin,                    // A, B1, B2, ...
        Mineral                     // Kalsium, jern, sink, ...
    }
}
